name: Test

on:
  workflow_call:
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Print branch
        run: |
          echo "Branch: ${{ github.head_ref }}"
